# PHP CRUD API Generator - Security Configuration
# 
# 🔒 IMPORTANT: Protect admin dashboard and health endpoint
# These files expose sensitive information and must not be publicly accessible
#
# 📖 Full security guide: docs/DASHBOARD_SECURITY.md

# Protect Admin Dashboard
<Files "dashboard.html">
    Order Deny,Allow
    Deny from all
    
    # ⚠️ REPLACE WITH YOUR IP ADDRESS
    # Find your IP: https://whatismyipaddress.com/
    Allow from 127.0.0.1              # Localhost (development)
    Allow from YOUR.IP.ADDRESS.HERE   # Replace this with your actual IP
    
    # Examples (remove # to enable):
    # Allow from 203.0.113.42         # Single IP
    # Allow from 198.51.100.0/24      # IP range (office network)
</Files>

# Protect Health Endpoint
<Files "health.php">
    Order Deny,Allow
    Deny from all
    
    # Allow same IPs as dashboard
    Allow from 127.0.0.1
    Allow from YOUR.IP.ADDRESS.HERE   # Replace this with your actual IP
    
    # For monitoring tools (Prometheus, etc.)
    # Allow from 198.51.100.10        # Monitoring server IP
</Files>

# Optional: Add HTTP Basic Authentication
# Uncomment and configure if you want password protection
#
# <Files "dashboard.html">
#     AuthType Basic
#     AuthName "Admin Dashboard"
#     AuthUserFile /path/to/.htpasswd
#     Require valid-user
# </Files>
#
# Create password file with:
# htpasswd -c .htpasswd admin

# Optional: Redirect HTTP to HTTPS (recommended for production)
# RewriteEngine On
# RewriteCond %{HTTPS} off
# RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]
